/** Slug de la ciudad por defecto (seed migración geo). */
export const GEO_DEFAULT_CITY_SLUG = 'rancagua' as const

/** UUID ficticio para mocks sin Supabase (no usar en inserts reales). */
export const MOCK_GEO_CITY_ID =
  '00000000-0000-4000-8000-000000000099' as const
