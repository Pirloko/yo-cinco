/**
 * Contenedor visual unificado para tarjetas de partido / reserva
 * (Inicio, Partidos, Explorar).
 */
export const MATCH_CARD_SHELL =
  'bg-card rounded-2xl border-2 border-border shadow-sm overflow-hidden ring-1 ring-border/40 hover:border-primary/55 hover:shadow-md hover:ring-primary/20 transition-all'

/** Filas compactas (p. ej. pendientes por cerrar) con el mismo lenguaje de borde. */
export const COMPACT_CARD_ROW =
  'rounded-xl border-2 border-border bg-card/60 shadow-sm ring-1 ring-border/40 hover:border-primary/55 hover:shadow-md transition-all'

/** Tabla con borde acorde (Explorar → horarios). */
export const TABLE_OUTLINE =
  'overflow-x-auto rounded-xl border-2 border-border bg-card/20 shadow-sm ring-1 ring-border/40'
